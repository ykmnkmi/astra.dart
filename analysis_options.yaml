include: package:lints/recommended.yaml

analyzer:
  exclude:
    - lib/src/cli/templates/**
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:
    avoid_print: true
    library_prefixes: false
    prefer_single_quotes: true
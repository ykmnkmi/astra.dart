name: example
description: A Shelf based Astra application.
version: 1.0.0
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  astra: ^1.0.0-dev.167

dev_dependencies:
  lints: ^6.1.0
